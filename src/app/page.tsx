
export default function Home() {
  return (
    /* Full bleed: no container, no gutter, no gap. The navy ground sits behind
       the tiles so any subpixel seam reads as a hairline rather than white. */
    <section className="bg-navy">
      
    </section>
  );
}
